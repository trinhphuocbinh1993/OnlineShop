@model List<OnlineShop.Models.CartItem>
<div class="cart">
    <p>
        Dụng cụ chợ lớn chào quý khách! <span>Giỏ hàng:</span><div id="dd" class="wrapper-dropdown-2">
    @Model.Count() sản phẩm @*- @(Model.Sum(x => x.Product.Price * x.Product.Quantity)) đ*@
    <ul class="dropdown">
        <li>you have no items in your Shopping cart</li>
    </ul>
</div>
    </p>
</div>
